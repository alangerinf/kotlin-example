package com.chatowl.data.repositories

import com.chatowl.data.api.chatowl.ChatOwlAPIClient

object ChatOwlAuthRepository {
    private val client = ChatOwlAPIClient

//    suspend fun register(registerBody: LoginBody): LoginData =
//        client.register(registerBody).data
//
//    suspend fun login(loginBody: LoginBody): LoginData =
//        client.login(loginBody).data

}