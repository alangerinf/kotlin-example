<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black1">

    <ImageView
        android:id="@+id/fragment_fullscreen_image_image_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical"
        android:adjustViewBounds="true"
        android:contentDescription="@null"
        tools:src="@drawable/background_exercise" />

    <FrameLayout
        android:id="@+id/fragment_fullscreen_image_layout_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top">

        <ImageButton
            android:id="@+id/fragment_fullscreen_image_button_close"
            style="@style/ChatOwlImageButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|top"
            android:layout_margin="@dimen/margin_16dp"
            android:contentDescription="@string/close"
            android:src="@drawable/ic_close_large"
            android:tint="@color/selector_white" />

    </FrameLayout>

</FrameLayout>